<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quickcash">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuickCash">
        <activity android:name=".UserManagement.SignUpActivity" />
        <activity
            android:name=".WelcomePage"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true" />
        <activity android:name=".UserManagement.LogInActivity" />
        <activity android:name=".Home.EmployeeHomeActivity" />
        <activity android:name=".Home.EmployerHomeActivity" />
        <activity android:name=".JobPosting.JobPostingDetailsActivity"></activity>
        <activity android:name=".JobPosting.JobPostingActivity"></activity>
        <activity android:name=".TaskList.TaskListActivity"></activity>
        <activity android:name=".UserManagement.preferencePage"></activity>

    </application>

</manifest>